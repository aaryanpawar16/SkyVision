# Use PyTorch CPU wheels
--extra-index-url https://download.pytorch.org/whl/cpu

# Web
fastapi==0.115.0
uvicorn==0.30.6
gunicorn==22.0.0
httpx==0.27.2
requests==2.32.3
python-multipart==0.0.9

# DB + data
mariadb==1.1.10
pandas==2.2.3
numpy==1.26.4            # safe with manylinux wheels and torch cpu
tqdm==4.66.5
pydantic==2.8.2          # fastapi deps

# ML / embeddings (CLIP via SentenceTransformers)
torch==2.3.1             # CPU build will be pulled from the extra index above
transformers==4.44.2
sentence-transformers==2.6.1
Pillow==10.4.0
