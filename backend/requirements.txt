# Ensure CPU wheels for PyTorch/TorchVision
--extra-index-url https://download.pytorch.org/whl/cpu

# Web
fastapi==0.115.6
uvicorn==0.30.6
gunicorn==22.0.0
httpx==0.27.2
requests==2.32.3
python-multipart==0.0.9

# DB + data
mariadb==1.1.10
pandas==2.2.3
numpy==1.26.4            # safe manylinux wheel widely compatible
tqdm==4.66.5
pydantic==2.9.2          # ok for FastAPI 0.115.x

# ML / embeddings (works on Python 3.13 w/ CPU)
torch==2.9.0+cpu
torchvision==0.20.0+cpu  # optional but good to have for image ops
transformers==4.45.2
sentence-transformers==3.2.1
Pillow==10.4.0
